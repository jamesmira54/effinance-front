export interface LoginFormProps {
    username: string;
    password: string;
}

export interface SignUpFormProps {
    firstName: string;
    lastName: string;
    username: string;
    email: string;
    mobileNumber: string;
    password: string;
    repassword: string;
}